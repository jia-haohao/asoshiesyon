<div class = "wrapper clearfix">
  <div class = "container">
  <h1>詳細画像<h1>
    <p>
      <strong>ユーザー:</strong>
      <%= @picture.user.name %>
    </p>

    <p>
      <strong>本文:</strong>
      <%= @picture.content %>
    </p>

    <p>
      <strong>画像</strong>
      <%= image_tag @picture.image.url, style: "width: 400px; height: 240px" if @picture.image_url %>
    </p>

    <% unless @picture.user_id == current_user.id %>
      <% if @favorite.present? %>
        <%= link_to "💖", favorite_path(id: @favorite.id), method: :delete, class: "btn btn-danger" %>
      <% else %>
        <%= link_to "💔", favorites_path(picture_id: @picture.id), method: :post, class: "btn btn-primary" %>
      <% end %>
    <% end %>

    <% if @user == current_user  %>
      <%= link_to edit_picture_path(@picture) do %>
        <span class="bi bi-pencil-square"></span><% end %>
    <% end %>
    <%= link_to pictures_path do %>
      <span class="bi bi-skip-backward-btn"></span><% end %>
  </div>
</div>